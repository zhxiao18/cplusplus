#include <iostream>
#include <string.h>
#include <stdio.h>

using std::cin;
using std::cout;
using std::endl;

template <typename T>
T add(T x, T y)
{
    cout << "add(T x, T y)" << endl;
    return x + y;
}

