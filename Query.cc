#include <iostream>
#include <string.h>
#include <string>
#include <vector>
#include <set>
#include <map>
#include <memory>
#include <sstream>
#include <fstream>

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;
using std::set;
using std::map;
using std::unique_ptr;
using std::shared_ptr;

class textDeal
{
public:
    
private:
    vector<string> _lines;
    map<string, set<int>> _word_lines;
    map<string, int> _word_times;
};

int main()
{   
    return 0;
}

